{
  "name": "@sidequest/core",
  "version": "0.1.0",
  "description": "Reusable core business logic: scheduler, storage, quest sides, gate mode",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/exercises && cp src/exercises/*.json dist/exercises/",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "tsx scripts/demo.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "tsx": "^4.19.0"
  }
}
